{
  "name": "draw-straws",
  "version": "0.1.0",
  "private": true,
  "description": "Stateless çöp çekme oyunu — Express statik sunucu + istemci crypto.getRandomValues",
  "type": "commonjs",
  "scripts": {
    "start": "node src/server.js",
    "test": "node --test",
    "test:coverage": "node --test --experimental-test-coverage"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jsdom": "^29.1.1",
    "supertest": "^7.0.0"
  }
}
