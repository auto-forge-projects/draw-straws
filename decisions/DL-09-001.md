# DL-09-001: Development uygulaması — dosya yapısı, TDD sırası, kısıtlar

- Tarih: 2026-07-19
- Faz: 9 — Development
- Mod: AUTOPILOT
- Karar: `docs/05-architecture.md`'deki bileşen tasarımı birebir uygulandı: `src/server.js` (Express static + `/health`, SEC-4/SEC-7 başlıkları), `public/shuffle.js` (CSPRNG + rejection sampling + Fisher-Yates + `createDraw`), `public/index.html` + `style.css` + `game.js` (S1/S2/S3 tek-sayfa akışı). Bir önceki koşum kesintiye uğramıştı (ORPHANED_RUN): `package.json` + `tests/shuffle.test.js` (kırmızı test, TDD adım 1) zaten diskte hazır bulundu; resume bu iş listesinden (TASK-001..006) devam etti, baştan üretmedi.
- Değerlendirilen alternatifler: (1) `public/` klasörünü `src/public/` altına almak (mimari diyagramdaki gibi) — ancak kesintiye uğramış koşumun bıraktığı `tests/shuffle.test.js` zaten `../public/shuffle.js` yolunu (repo kökünde `public/`) referans alıyordu; iş listesinden devam ilkesi (AF-038) gereği var olan teste uyuldu, dosya taşınmadı. (2) Yeniden Çek sonrası kullanıcıyı S1'e geri götürmek — ancak akış diyagramı ve FR-4 kabul kriteri "aynı N ile yeni bağımsız karıştırma+çekiliş başlar" dediği için doğrudan S2'ye geçildi (tabloyla diyagram çelişiyordu, diyagram+FR tercih edildi).
- Gerekçe: Var olan (disk üzerindeki) TDD kırmızı testini değiştirmek/silmek disiplin kaybı olurdu; test zaten FR-2/SEC-1/SEC-2'yi doğru şekilde kapsıyordu. `crypto.getRandomValues` global (window yerine) kullanıldı çünkü hem tarayıcıda hem Node test ortamında (Node 22, global `crypto`) çalışır — SEC-1'in CSPRNG şartını bozmadan test edilebilirlik kazandırır.
- Riskler: `public/game.js` DOM-bağımlı olduğundan otomatik birim testi yok (yalnız manuel/entegrasyon doğrulaması); ileride jsdom eklenirse kapsanabilir — düşük risk, LITE kapsamında kabul edilebilir.
- Geri alınabilirlik: Yüksek (statik dosyalar + tek server.js, bağımlılık express tek paket; her şey kolayca değiştirilebilir).
- İnsan onayı: Otomatik (AUTOPILOT)
- Varsayım mı?: Evet — AUTOPILOT varsayımı: "Yeniden Çek" S2'ye doğrudan döner (S1'e değil), çünkü FR-4 kabul kriteri ve akış diyagramı bunu işaret ediyor; kullanıcı isterse S1'e dönüşe revize edilebilir.
